# Local agent memory

`memory.jsonl` is generated by the project MCP memory server and excluded from
Git. Back it up with the project if its recalled context matters.

Do not store secrets or credentials here. Write durable project facts to the
versioned documents under `docs/` as well.
