# Claude Code project instructions

Before every task, read and follow `AGENTS.md` and the versioned project memory
it references. `AGENTS.md` is the canonical cross-agent contract.

Use the `project-memory` MCP server for relevant recall and durable non-secret
facts. Keep authoritative decisions, project state, and change reasons in Git.
